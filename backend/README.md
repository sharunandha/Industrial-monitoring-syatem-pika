# Backend Notes

- Uses Express, ThingSpeak proxy, JWT auth, and role-based access.
- Device authentication uses x-device-key or deviceKey (ThingSpeak Write API key).
- Admin login uses ADMIN_EMAIL and ADMIN_PASSWORD from env.
